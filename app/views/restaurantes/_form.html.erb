<%= form_with(model: restaurante, local: true, :class => "form") do |form| %>

  <% if restaurante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurante.errors.count, "error") %> prohibited this restaurante from being saved:</h2>

      <ul>
      <% restaurante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome_do_restaurante %><br>
    <%= form.text_field :nome, id: :restaurante_nome %><br>
  </div>

  <div class="actions">
    <%= form.submit "Salvar", :class => "btn btnGreen iconSave"%>
  </div>
<% end %>
